namespace ui;

test

import 'Button.loom';
import 'types.loom';

testkdkjdkj

type Size = 'small' | 'medium' | 'small';
type ExtraSize = Size | 'huge';
type Intent = 'danger';

bla

type Word = string;

class Button {

	@size: Size = 'medium';
	@intent: Intent | 'flashy' = 'flashy';

	slot icon;
	slot link;

	{%
		position: relative;
		width: 100px;
		height: 90px;
		animation: heartbeat 1s infinite;
	%}
}

class Link extends Button {

}

class Label {{%
	color: red;
%}}